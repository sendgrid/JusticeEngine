# These libraries are required on top of the required libraries within Security Monkey
arrow>=0.12.1
bandit>=1.4.0
sendgrid>=5.3.0
starkbank-ecdsa>=2.0.1 # not directly required, pinned by Snyk to avoid a vulnerability
